/** Point d’entrée unique de l’ancienne console admin (SPA / HTML). */
export const LEGACY_ADMIN_URL =
  "https://www.le-repasseur.fr/pricing/Admin.html";

export function legacyAdminUrl(): string {
  return LEGACY_ADMIN_URL;
}
